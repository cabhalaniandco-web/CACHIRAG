{
  "name": "CACHIRAG",
  "user": "ubuntu",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "install": "python3 -m venv .venv && . .venv/bin/activate && python -m pip install --upgrade pip && pip install -r requirements.txt",
  "terminals": [
    {
      "name": "api",
      "command": ". .venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    }
  ],
  "ports": [
    {
      "name": "api",
      "port": 8000
    }
  ]
}
